Usage:
```
pip install polib

msg2po -i INPUT_FILE [-o OUTPUT_FILE] [--encoding ENC]
po2msg source.po only_filename.msg
msg2msgstr dest.po full_path_to_occurence_in_po.msg source.msg msg_encoding msg_context
```
